package edu.berkeley.aep;

import java.util.*;

// understands how to combine groups
public class CombineGroups {

    // understands how to represent a group
    static class Group {
        String groupName;
        Integer groupSize;
        public Group(String groupName, Integer groupSize) {
            groupName = groupName;
            groupSize = groupSize;
        }
    }

    public int getAllowedNumOfGroups(List<Group> groups) {
        return 0;
    }
}
